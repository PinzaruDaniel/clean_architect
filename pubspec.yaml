name: clean_architect
description: A configurable CLI generator for clean architecture Flutter and Dart feature modules.
version: 0.1.2
repository: https://github.com/PinzaruDaniel/clean_architect

environment:
  sdk: ^3.6.0

dependencies:
  args: ^2.6.0
  collection: ^1.19.0
  mason_logger: ^0.3.1
  meta: ^1.16.0
  path: ^1.9.1
  yaml: ^3.1.2

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8

executables:
  clean_architect:
